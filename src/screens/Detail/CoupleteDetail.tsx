import React, { StatelessComponent } from 'react';
import { DetailSlider } from '../../components/Detail';

const CoupleteDetail: StatelessComponent<void> = () => (
  <DetailSlider
    title='couplete'
    maxStep={6}
  />
);

export default CoupleteDetail;
